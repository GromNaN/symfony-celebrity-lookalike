# Stage the modified README.md file
git add README.md

# Create a commit with a descriptive message
git commit -m "Update README to highlight MongoDB PHP Team ownership and demo purpose"

# Push the changes to the remote repository
git push
